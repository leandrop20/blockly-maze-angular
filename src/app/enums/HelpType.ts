export enum HelpType {

	NULL = 'null',
	STACK = 'stack',
	ONE_TOP_BLOCK = 'oneTopBlock',
	RUN = 'run',
	RESET = 'reset',
	REPEAT = 'repeat',
	CAPACITY = 'capacity',
	REPEAT_MANY = 'repeatMany',
	IF = 'if',
	MENU = 'menu',
	IF_ELSE = 'ifElse'

}